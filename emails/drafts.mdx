---
title: "Email drafts"
description: "Create a draft from a template and customize before sending"
icon: "pen-to-square"
---

A **draft** is an email ready to send. You create it from a [template](/emails/templates), optionally edit the content, set **custom variables**, then choose [recipients](/emails/recipients) and [send](/emails/sending).

## Create a draft

1. Go to **Emails** → **Drafts** and click **Create draft** (or **New draft**).
2. Select a **template**. The draft is pre-filled with that template's subject and body.
3. Give the draft a **name** (e.g. "March newsletter send").
4. Optionally **edit** the subject or body (changes apply only to this draft, not the template).
5. Set **custom variables** (if the template uses them): e.g. `event_date` = "March 15", `link_url` = "https://...". These override the placeholders when sending. See [Variables](/emails/variables).
6. Save. The draft is ready to [send](/emails/sending).

<Tip>
Use custom variables for one-off values (e.g. event date, specific link) so you do not edit the template every time.
</Tip>

## Edit a draft

- Open the draft from **Drafts**, change subject, body, or custom variables, and save. Until you send, you can keep editing. After sending, the draft remains for history; you can duplicate it to create a new draft from the same content if the UI allows.

## Send from a draft

- Open the draft and click **Send** (or **Send email**). Then choose [recipients](/emails/recipients) (e.g. a [group](/groups/using-groups-in-emails), all members, or a custom list) and confirm. See [Sending](/emails/sending).
